import React, { useState } from 'react';
import NavbarPresentational from './NavBarPresentational';

const NavbarContainer: React.FC<any> = (props) => {
  return (
    <NavbarPresentational />
  );
};

export default NavbarContainer;